"""nuway ML library (M0): geometry twins, rig parsing, datasets, models, training.

Never imports ``rclpy``; must import in an environment without ROS and, for the
``common`` subpackage, without torch (``docs/03_style_and_conventions.md`` §6.1).
"""
