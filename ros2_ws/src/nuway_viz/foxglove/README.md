# Foxglove layouts

`bev_m0.json` (M0 task 12): map, ego, reference line, lookahead point.
Layouts are for a human at the devbox; every drawn layer has a headless
matplotlib twin under `ml/nuway_ml/viz/` (M1 task 13).
