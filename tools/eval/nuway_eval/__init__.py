"""nuway_eval: THE evaluation harness package (rclpy; lives under tools/, docs/01 Rules).

M0 ships ``route_runner`` v0 (``M0_bringup.md`` §2.11): one stack per town,
reset + waypoints per route, lateral-error statistics from ControlDebug. M1
adds infractions, driving score, MCAP recording and the report renders.
"""
